complex_default: &default
  access_key_id: access_key
  secret_access_key: secret_access_key

complex_development:
  bucket: develop
  <<: *default

complex_production:
  bucket: production
  <<: *default

complex_test: &test